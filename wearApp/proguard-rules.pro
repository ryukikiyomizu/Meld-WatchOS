# Keep rules minimal for now; tighten once the Wear UI stabilizes.
#
# The Data Layer protocol models in `wearbridge/` are serialized by hand through org.json, so no
# reflection-based keep rules are required.
